import java.util.*

fun main()
{
    /*val scan = Scanner(System.`in`)
    var lista: MutableList<Int> = mutableListOf<Int>()

    for(num in 1..5)
    {
        println("Digite um número: ")
        lista.add(scan.nextInt())
    }

    var soma: Float = 0.0F
    for(args in lista)
    {
        soma += args
    }

    var media: Float

    media = soma/5

    println(media)
    */

    val lista = arrayOf("Yellow", "Blue", "Green", "Red")

    

}